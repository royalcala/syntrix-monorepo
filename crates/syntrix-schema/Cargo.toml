[package]
name = "syntrix-schema"
version = "0.1.0"
edition = "2021"
description = "Schema registry for Syntrix ERP entities — source of truth for indexes, search, migrations, and UI codegen"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
